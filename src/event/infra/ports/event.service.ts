export abstract class EventService {
  // abstract create(event: EventM): Promise<EventM>;
  // abstract findAll(): Promise<EventM[]>;
  // abstract findOne(id: string): Promise<EventM>;
  // abstract update(id: string, event: EventM): Promise<EventM>;
  // abstract delete(id: string): Promise<EventM>;
  // abstract publish(id: string): Promise<EventM>; // перевести в статус опубликовано
  // abstract reschedulie(id: string, newDate: Date): Promise<EventM>;
  // abstract addParticipant(
  //   eventId: string,
  //   userId: string,
  // ): Promise<SubscriptionM>;
  // abstract removeParticipant(eventId: string, userId: string): Promise<void>;
  // abstract listParticipants(eventId: string): Promise<SubscriptionM[]>;
}
